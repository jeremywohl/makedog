// Version identity for makedog, bumped by hand each release.
package main

// version must stay in step with the release's git tag; the tap-release
// workflow refuses a release where the two disagree.
const version = "0.1.0"
